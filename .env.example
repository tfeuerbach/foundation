# ----------------------------------------------------
# General Project Configuration
# ----------------------------------------------------
# A name for the docker-compose project to prevent conflicts with other projects.
COMPOSE_PROJECT_NAME=project-name
# Set your timezone to ensure proper log timestamps and for services that rely on it.
# A list of valid timezones can be found here: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# ----------------------------------------------------
# Public Facing Website Configuration
# ----------------------------------------------------
# The name of your company, which will be displayed on the public website.
PUBLIC_COMPANY_NAME="Company"
# The name of your primary brand/business, displayed below the company name.
PUBLIC_BRAND_NAME="Brand Name"

# ----------------------------------------------------
# Traefik & Domain Configuration
# ----------------------------------------------------
# Your primary domain name. This will be used for the public site and all subdomains.
DOMAIN_NAME=yourdomain.com
# The email address to be used for Let's Encrypt SSL certificate registration and notifications.
LETSENCRYPT_EMAIL=letsencrypt@yourdomain.com

# ----------------------------------------------------
# Authentik (Authentication Service)
# ----------------------------------------------------
# PostgreSQL Credentials (as expected by the official Authentik image)
PG_USER=${POSTGRES_USER:-authentik}
PG_PASS=${POSTGRES_PASSWORD:-PLEASE_CHANGE_ME_TO_A_SECURE_PASSWORD}
PG_DB=${POSTGRES_DB:-authentik}

# Authentik Core Settings
# A strong, randomly generated secret key for Authentik. You can generate one with: openssl rand -base64 60
AUTHENTIK_SECRET_KEY=PLEASE_CHANGE_ME_TO_A_RANDOM_SECRET_KEY

# Authentik Email Configuration (SMTP)
# These are used by authentik to notify you about alerts and for user verification/recovery.
AUTHENTIK_EMAIL__HOST=localhost
AUTHENTIK_EMAIL__PORT=25
AUTHENTIK_EMAIL__USERNAME=
AUTHENTIK_EMAIL__PASSWORD=
AUTHENTIK_EMAIL__USE_TLS=false
AUTHENTIK_EMAIL__USE_SSL=false
AUTHENTIK_EMAIL__TIMEOUT=10
AUTHENTIK_EMAIL__FROM=authentik@localhost

# ----------------------------------------------------
# UVDesk (Helpdesk Service)
# ----------------------------------------------------
# Credentials for the database that will be used by UVDesk.
# UVDesk will be configured to use a separate PostgreSQL database.
UVDESK_DB_USER=uvdesk
UVDESK_DB_PASSWORD=PLEASE_CHANGE_ME_TO_A_SECURE_PASSWORD
UVDESK_DB_NAME=uvdesk
